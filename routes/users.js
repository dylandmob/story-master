const express = require('express');
const router = express.Router();
const User = require('../models/User');

/* TODO 
  - add getting users
  - add getting user by id
  - add deleting user
*/

module.exports = router;
